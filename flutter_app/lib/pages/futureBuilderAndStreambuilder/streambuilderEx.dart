import 'package:flutter/material.dart';

class StreamBldPage extends StatelessWidget {
  const StreamBldPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('StreamBldPage'),
        ),
        body: Text('13232323'));
  }
}

Stream getData() {}
